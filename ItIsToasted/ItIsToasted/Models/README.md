Place the on-device Gemma model file here for the first iPhone runtime test.

Expected filename:

`gemma-phone-local.bin`

The app looks for this bundled resource path:

`Models/gemma-phone-local.bin`

This file is intentionally ignored by git in the repo root `.gitignore`.
